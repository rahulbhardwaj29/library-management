# library-management
This is Library Admin CLI (Command-Line Interface) built with Python. It allows a user to manage a collection of books by providing a text-based menu. All book data is saved to and loaded from a local library_data.json file, ensuring data persistence between sessions.
